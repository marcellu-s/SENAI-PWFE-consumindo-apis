class Pokemon {
    constructor(id, name, types, primaryType, sprite) {
        this.id = id;
        this.name = name;
        this.types = types;
        this.primaryType = primaryType;
        this.sprite = sprite;
    }
}